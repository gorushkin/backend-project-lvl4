extends ../layouts/application.pug

block header
  = t('views.statuses.createTitle')

block content
  +formFor()(action=route('statuses') method='post')
    +input(status, 'name', errors)(type="text")
    +submit(status)(value=t('views.users.new.submit'))
